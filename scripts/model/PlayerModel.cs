using UnityEngine;
using System.Collections;
using System.Collections.Generic;

/// <summary>
/// Player model.
/// the player model some day will be different
/// this monobehavior will become the communication gameobject that takes charge
/// if the directly communication with the http request or call or webservice or database
/// or playerstats or whatever it takes to get the information
/// 
/// and then the Player 
/// </summary>
public class PlayerModel : MonoSingleton<PlayerModel>
{	
	
}
